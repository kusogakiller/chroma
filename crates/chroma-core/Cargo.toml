[package]
name = "chroma-core"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
description = "Chroma protocol core: constants, types, canonical serialization"

[dependencies]
blake3 = "1.5"
bech32 = "0.11"
hex = "0.4"
thiserror = "1.0"

[dev-dependencies]
proptest = "1.5"